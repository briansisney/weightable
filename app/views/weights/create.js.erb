(function () {
  var weightHtml = "<%= j render(partial: 'weight', locals: { weight: @weight, comment:@comment }) %>";
  console.log(weightHtml);
  $('.weights').prepend(weightHtml).find('.weight_button').click( function (e) {
    e.preventDefault();
    $(this).closest('.form').children('.form_div').toggleClass('show_form');
  // $(this).toggleClass('hide')
  });;
  $('#weight_weight').val("");

  
  // <% if remotipart_submitted? %>
  //   console.log('submitted via remotipart')
  // <% else %>
  //   console.log('submitted via native jquery-ujs')
  // <% end %>
})();