(function () {
  var weightHtml = "<%= j render(partial: 'weight', locals: { weight: @weight }) %>";
  $('.weights').prepend(weightHtml);
  $('#weight_weight').val("");

  
  // <% if remotipart_submitted? %>
  //   console.log('submitted via remotipart')
  // <% else %>
  //   console.log('submitted via native jquery-ujs')
  // <% end %>
})();